<!DOCTYPE html>

<html>
<head>
    <title>Jeu</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" 
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
</head>

<body>

<div class="container text-center">

    <h1>LE JEU DU JUSTE PRIX</h1>

    <div class="row col-8 mt-4 mx-auto">

        <div class="card shadow">

            <form action="/game/check/" method="post" class="card-body" id="formPlayGame">

                <div class="mb-4" id="info">
                    Bonjour , vous devez trouver la valeur de l'objet suivant en un minimum de tentatives: 
                    <ul>
                        <li>Joueur: <%= playerName %></li>
                        <li>Objet: <%= objectName %></li>
                    </ul>
                </div>

                <% if (guess != 'equals') { %>
                    <div class="input-group mb-3">
                        <input type="text" name="newTry" class="form-control" placeholder="Quel prix vous semble être juste ?" id="playerTwoTry">
                        <button type="submit" id="submitFormPlayGame" class="btn btn-outline-secondary">Faire une tentative</button>
                    </div>
                <% } %>

                <% if (guesses > 0) { %>
                    <div id="tries">
                        <% if (guess == 'higher') { %>
                            <p class="alert-warning">La valeur de l'objet est plus haute</p>
                        <% } %>
                        
                        <%if (guess == 'lower') { %>
                            <p class="alert-warning">La valeur de l'objet est plus basse</p>
                        <% } %>

                        <% if (guess == 'equals') { %>
                            <p class="alert-success">Félicitations ! Tu as trouvé la bonne valeur en seulement <%= guesses %> tentative(s)</p>
                        <% } %>
                    </div>

                    <% if (guess == 'equals') { %>
                        <a href="/?restart=true" type="button" id="replayGame" class="btn btn-dark">Rejouer</a>
                    <% } %>
                <% } %>

            </form>

        </div>

    </div>

</div>

</body>
</html>
